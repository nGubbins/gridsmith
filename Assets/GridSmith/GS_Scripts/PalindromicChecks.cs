using UnityEngine;

public class PalindromicChecks : MonoBehaviour
{

/// <summary>
/// checks for palindromic pattern in rows, columns, and diagonals
/// </summary>

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
