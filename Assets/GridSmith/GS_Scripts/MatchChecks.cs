using UnityEngine;

public class MatchChecks : MonoBehaviour
{
    /// <summary>
    /// ToDo: Functions to check for rows, columns, and diagonals where all tiles match
    /// </summary>

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //check if two columns match

    //check if two rows match

    //check if all tiles in a row match

    //check if all tiles in a column match

    //check if all tiles in a diagonal match
}
